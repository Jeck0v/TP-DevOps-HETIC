1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
  http://{{ .Values.ingress.host }}
{{- else }}
  kubectl port-forward svc/{{ include "devops-app.fullname" . }} 8080:{{ .Values.service.port }}
  http://localhost:8080
{{- end }}
